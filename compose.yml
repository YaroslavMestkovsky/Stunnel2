version: '3'

services:
  ubuntu-client:
    build:
      context: .
      dockerfile: ./client/Dockerfile
    image: ubuntu:client
    container_name: client
    networks:
      - bridge
    depends_on:
      - nginx-server
    ports:
      - "443:443"

  nginx-server:
    build:
      context: .
      dockerfile: ./server/Dockerfile
    image: nginx:server
    container_name: server
    networks:
      - bridge

networks:
  bridge:
